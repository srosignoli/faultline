apiVersion: v1
kind: Service
metadata:
  name: faultline-controller
  namespace: faultline
spec:
  type: ClusterIP
  selector:
    app: faultline-controller
  ports:
  - port: 80
    targetPort: 8080
